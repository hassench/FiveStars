@model IEnumerable<Entities.Models.Theme>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Passinggrade)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.info)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Passinggrade)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.info)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ThemeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ThemeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ThemeId })
        </td>
    </tr>
}

</table>
